name: Deploy to Azure

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Azure Login
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    - name: Verify Azure Login
      run: |
        az account show || (echo '❌ Azure secrets missing or invalid. Check AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID in repo Settings → Secrets.' && exit 1)

    - name: Create and Configure Azure Storage Account
      run: |
        EXISTING_LOCATION=$(az group show --name staticazuretest3d6f0f-rg --query location -o tsv 2>/dev/null || echo '')
        LOCATION=${EXISTING_LOCATION:-eastus}
        az group create --name staticazuretest3d6f0f-rg --location $LOCATION || true
        az storage account create --name staticazuretest3d6f0f --resource-group staticazuretest3d6f0f-rg --location $LOCATION --sku Standard_LRS --allow-blob-public-access true || true
        az storage blob service-properties update --account-name staticazuretest3d6f0f --auth-mode login --static-website --404-document index.html --index-document index.html

    - name: Upload to Azure Storage $web container
      run: az storage blob upload-batch -d '$web' --account-name staticazuretest3d6f0f --auth-mode login -s dist --overwrite

    - name: Notify Success
      if: success()
      run: |
        curl -X POST https://rebuttal-slashing-glove.ngrok-free.dev/api/UnifiedDeployment/webhook/github -H "Content-Type: application/json" -d '{"deploymentId": "6a22e1bb9811a4c4c0a1bcd6", "status": "success"}'

    - name: Notify Failure
      if: failure()
      run: |
        curl -X POST https://rebuttal-slashing-glove.ngrok-free.dev/api/UnifiedDeployment/webhook/github -H "Content-Type: application/json" -d '{"deploymentId": "6a22e1bb9811a4c4c0a1bcd6", "status": "failed"}'
